Các file trong thư mục này là cấu hình thí nghiệm cũ/không nằm trong flow hiện tại.

Mục tiêu dọn repo:
- Giữ các config “đang dùng” ở `configs/experiment/` (paper reproduction + backbone swap sau).
- Các config cũ được chuyển vào `_archive/` để tránh nhầm lẫn, nhưng không bị xoá.

